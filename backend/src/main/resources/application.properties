server.port=8080

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/myphone?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&nullCatalogMeansCurrent=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}

# Connection pool (quan trọng trên Render free tier - giảm connection để tránh timeout)
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:5}
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

spring.servlet.multipart.max-file-size=${MULTIPART_MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MULTIPART_MAX_REQUEST_SIZE:10MB}

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:none}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.hbm2ddl.jdbc_metadata_extraction_strategy=individually

# Transaction timeout configuration - MUST FIX #7
spring.transaction.default-timeout=30
spring.jpa.properties.hibernate.query.timeout=10000

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000,https://myphone-store-admin.vercel.app}

jwt.secret=${JWT_SECRET:bXlwaG9uZS1kZXYtc2VjcmV0LWtleS0zMi1ieXRlLWF0LWxlYXN0ISE=}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
google.client-id=${GOOGLE_CLIENT_ID:1091375226649-tipibo3vdvk3n2hb0ptepksv0tcmqecs.apps.googleusercontent.com}
groq.api-key=${GROQ_API_KEY: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}
groq.model=${GROQ_MODEL:llama-3.3-70b-versatile}

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=${REDIS_TIMEOUT:2000}
# Upstash Redis yêu cầu SSL - chỉ set khi có REDIS_URL
spring.data.redis.ssl.enabled=${REDIS_SSL:false}

minio.endpoint=${MINIO_ENDPOINT:http://localhost:9000}
minio.access-key=${MINIO_ACCESS_KEY:root}
minio.secret-key=${MINIO_SECRET_KEY:12345678}
minio.bucket-name=${MINIO_BUCKET_NAME:myphone}
minio.region=${MINIO_REGION:}
minio.url-prefix=${MINIO_URL_PREFIX:http://localhost:8080/api/files}

# openai.api-key=${OPENAI_API_KEY:}
# openai.model=${OPENAI_MODEL:gpt-4o-mini}

# ai.provider=${AI_PROVIDER:ollama}
# ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
# ollama.model=${OLLAMA_MODEL:phi3:latest}

# Mail (Gmail SMTP) - use App Password
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:buivanha22032004@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nwmabsdecwjzrexr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000
spring.mail.properties.mail.debug=true

mail.from=${MAIL_FROM:${MAIL_USERNAME:}}

# Actuator - expose health với chi tiết để debug
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
management.health.mail.enabled=true

# Giảm RAM usage cho Render free tier 512MB
spring.jpa.open-in-view=true
server.tomcat.max-threads=20
server.tomcat.min-spare-threads=5
spring.datasource.hikari.maximum-pool-size=3
